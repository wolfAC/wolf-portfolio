// Central place for the branding strings that used to be hardcoded as
// "Bruno Simon" throughout the project. Edit these to rebrand -- the tab
// title, console signature, and social links all read from here. index.html
// is plain static markup (not a JS module), so its title/meta tags and the
// home bio card can't import this file directly; keep those in sync by hand.
export const AUTHOR_NAME = 'Wolf'
export const SITE_TITLE = `${AUTHOR_NAME}'s`
export const SITE_DESCRIPTION = `${AUTHOR_NAME}'s creative portfolio`
export const REPO_URL = 'https://github.com/wolfAC/wolf-portfolio'
